apiVersion: v1
kind: Service
metadata:
  name: {{ include "hello-world-maven.fullname" . }}
  labels:
    {{- include "hello-world-maven.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hello-world-maven.selectorLabels" . | nindent 4 }}